<Window x:Class="EasyGui.Views.SettingsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:EasyGui.ViewModels"
        mc:Ignorable="d"
        Title="Paramètres"
        Height="600"
        Width="800">
    <Window.DataContext>
        <vm:SettingsViewModel />
    </Window.DataContext>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>

        <!-- Section principale des paramètres -->
        <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Vertical">
            <TextBlock Text="Paramètres généraux" FontWeight="Bold" FontSize="18" Margin="0,0,0,10" />
            <TextBlock Text="Langue :" />
            <ComboBox SelectedItem="{Binding Language}">
                <ComboBoxItem Content="Français" Tag="fr" />
                <ComboBoxItem Content="Anglais" Tag="en" />
            </ComboBox>
            <TextBlock Text="Format du journal :" Margin="0,10,0,0" />
            <ComboBox SelectedItem="{Binding LogFormat}">
                <ComboBoxItem Content="JSON" />
                <ComboBoxItem Content="XML" />
            </ComboBox>
        </StackPanel>

        <!-- Menu de gestion des extensions -->
        <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Vertical" Margin="10,0,0,0">
            <TextBlock Text="Extensions prioritaires" FontWeight="Bold" FontSize="18" Margin="0,0,0,10" />

            <TextBox Text="{Binding NewExtension}" Width="200" PlaceholderText="Entrez une extension (.txt)" Margin="0,0,0,5" />
            <Button Content="Ajouter une extension" Command="{Binding AddPriorityExtensionCommand}" Width="200" Margin="0,5,0,5" />

            <ListBox ItemsSource="{Binding PriorityExtensions}" Height="150" Width="250">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding}" Margin="0,0,10,0" />
                            <Button Content="X" Command="{Binding DataContext.RemovePriorityExtensionCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                    CommandParameter="{Binding}" />
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
    </Grid>
</Window>
